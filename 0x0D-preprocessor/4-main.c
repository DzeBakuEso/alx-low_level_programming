#include "main.h"
#include "4-sum.h"

/**
 * main - Check the code for the macro SUM
 *
 * Return: Always 0.
 */
int main(void)
{
    int s;

    s = SUM(98, 1024);  /**< Calculate the sum of 98 and 1024 */
    printf("%d\n", s);  /**< Print the result */
    return (0);
}

